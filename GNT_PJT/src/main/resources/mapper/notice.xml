<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="ns.sql.NoticeMapper">
	<select id="getNoticeList" resultType="notice">
		SELECT notice_id, user_id, notice_title, create_time, view_cnt
		FROM notice
	</select>
	<select id="getNoticeDetail" resultType="notice" parameterType="int">
		SELECT notice_id, user_id, notice_title, notice_content, create_time, view_cnt
		FROM notice
		WHERE notice_id = #{VALUE}		
	</select>
</mapper>